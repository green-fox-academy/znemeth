#include <iostream>
#include <string>

int main(int argc, char* args[]) {

    for (int a=0; a<501; a = a + 2) {
        std::cout << a << " " << std::endl;
    }
    // Create a program that prints all the even numbers between 0 and 500

    return 0;
}